---
import { cn } from "@/lib/utils";
interface Props {
  className: string;
}

const { className } = Astro.props;
---

<div class={cn(`mx-auto w-full max-w-screen-xl px-2.5 md:px-20`, className)}>
  <slot />
</div>

<!-- <div class={cn(`mx-auto w-full max-w-screen-xl px-0 min-[320px]:px-10 sm:px-20 md:px-20`, className)}>
  <slot />
</div> -->